import React, { Component } from '@alipay/bigfish/react';
import { connect } from '@alipay/bigfish/sdk';

import styles from './{{model.name}}.less';

@connect(({ {{model.varName}}Ns }: any) => ({}))
export class {{model.name}} extends Component {
  render() {
    return <div className={styles.{{model.varName}}}>{{model.name}}</div>;
  }
}
